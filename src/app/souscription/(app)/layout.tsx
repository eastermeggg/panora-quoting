import { AppLayout } from "@/components/layout/AppLayout";

export default function SouscriptionLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <AppLayout>{children}</AppLayout>;
}
